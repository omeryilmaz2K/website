# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Firebase Configuration
# You can either use individual config values or the service account JSON
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Option 1: Use Service Account JSON (recommended for production/Vercel)
# Paste your entire service account JSON as a single-line string
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# Option 2: Use Service Account Key File (for local development)
# Place your serviceAccountKey.json file in backend/config/ directory
# The app will automatically use it if FIREBASE_SERVICE_ACCOUNT is not set
